title("Toxic Spores")
types(evil)
trigger(onPlay, do(heal(1)));
trigger(onPlay, do(addStatus(bleed,1)));
trigger(onPlay, do(purge,(self),false));
cost(0);
text("Heal 1, gain 1 bleed, Purge");
removetrigger(endofturn, true, 0);
rarity(0)
image("res://Images/CardArt/toxicspores.png")

title("Dazed")
types(evil)
cost(-);
trigger(onRetain, do(purge,(self)))
text("Unplayable. on retain, purge")
modifiers(retain,unplayable)
rarity(0)
removetrigger(endofturn, true, 0);
image("res://Images/CardArt/stunned.png")

title("Sear")
types(fire,evil)
cost(-);
removetrigger(endofturn,true,4);
text("At the end of your turn burn a card from your hand or lose 3 health")
interrupt(endofturn, if((1=0),(false)));
interrupt(endofturn, do(create("Ouch!",Hand)));
interrupt(endofturn, do(voided,((select(Hand,true,"pick a card to burn (Sear)")),Hand),false));
interrupt(endofturn, do(purge((select(Hand,(hasName(self,"Ouch!")),"-",1,true)))));
trigger(onRemoveFromPlay,do(purge(self)))
image("res://Images/CardArt/Sear.png")
rarity(0)

title("Ouch!")
types(evil)
cost(-);
removetrigger(endofturn,true,0)
trigger(onVoided, do(heal(-3),true))
trigger(onVoided, do(purge(self)))
text("When burned: purge this and lose 3 health. Otherwise Purge")
image("res://Images/CardArt/Ouch.png")
rarity(0)

title("Curse of Vanishing")
trigger(onDraw,do(discard(self)));
text("When drawn, this discards itself. On play, purge")
trigger(onPlay, do(purge(self)))
types(cloud, evil,earth)
modifiers(curse)
rarity(0)
cost("0")
removetrigger(endofturn,true,0)

title("Frostbite")
types(reaction,evil)
trigger(onDraw, do(addStatus(frost,2)));
trigger(onPlay, do(purge,(self),false));
cost(1);
text("When drawn gain 2 Frost. When played, purge");
removetrigger(endofturn, true, 0);
rarity(0)

title("Potion Purchase")
types(potion, loot)
rarity(0)
modifiers(temporary)
cost(1)
text("Purge a card from your hand, choose 1 of 3 potions to add to your hand. The potion seller leaves.\n")
trigger(onPlay,do(purge((select(Hand,true,"Pick a card to purge")))))
trigger(onPlay, do(cardreward,("potion",3)))
trigger(onPlay, enemyDo(say( (do(selectTiles( (1,(any),"name:Potion Seller"),40,Player))), "Bye")))
trigger(onPlay, enemyDo(kill( (do(selectTiles( (1,(any),"name:Potion Seller"),40,Player))), null)))
removetrigger(endofturn, true,1);
image("res://Images/CardArt/PotionPurchase.png")

