title(Shadow Slice);
text("Deal $Damage shadow slash damage to an adjacent enemy.\n Discard from play immediately");
cost(0);
trigger(onPlay, do(damage, ($Damage,(shadow,slash),(any),$Range),false))
trigger(onPlay, do(move, (Play, Discard, self), false));
types(attack, shadow);
removetrigger(endofturn, true,0);
image("res://Images/CardArt/ShadowSlice.png");
rarity(7);
$Damage=6
$Range=1

title(Ominous Wind);
cost(0)
$Draw=1
trigger(onPlay,do(draw($Draw)))
trigger(startofturn,do(draw($Draw)))
types(shadow,wind)
text("Now and at the start of your next turn, draw $Draw")
removetrigger(endofturn,true,2)
image("res://Images/CardArt/OminousWind.png");
rarity(10)

title(Shadow Self);
$Times=3
cost(0)
trigger(onPlay, repeat( (do(createByMod((shadow),Hand))),$Times))
removetrigger(endofturn,true,1)
types(shadow)
text("Add $Times random shadows to your hand")
image("res://Images/CardArt/ShadowSelf.png");
rarity(10)

title(Fluttering Shadow);
cost(0)
$Draw=1
types(shadow,wind)
removetrigger(endofturn,true,0)
trigger(onPlay, do(draw($Draw)))
trigger(onPlay, do(purge(self)))
text("Draw $Draw, purge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/flutteringshadow.png");

title(Violent Shadow);
cost(0)
$Damage=2
$Range=2
types(shadow,attack)
trigger(onPlay, do(damage($Damage,(shadow),(any),$Range)))
trigger(onPlay, do(purge(self)))
removetrigger(endofturn,true,0)
text("Deal $Damage damage, range $Range.\npurge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/violentshadow.png");

title(Stoic Shadow);
cost(0)
$Block=2
types(shadow)
trigger(onPlay, do(block($Block)))
trigger(onPlay, do(purge(self)))
removetrigger(endofturn,true,0)
text("Block $Block .\npurge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/stoicshadow.png");

title(Clouded Shadow);
cost(0)
$Block=2
types(shadow,cloud)
trigger(onPlay, do(discard,((select(Hand,true,"pick a card to discard"))),false));
trigger(onPlay, do(purge(self)))
removetrigger(endofturn,true,0)
text("Discard a card.\nPurge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/cloudedshadow.png");

title(Burning Shadow);
cost(0)
$Draw=1
types(shadow,fire)
removetrigger(endofturn,true,0)
trigger(onPlay, do(voided((select(Hand,true,"pick a card to burn")),Hand)))
trigger(onPlay, do(purge(self)))
text("Burn a card from your hand.\nPurge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/burningshadow.png");

title(Repulsive Shadow);
cost(0)
$Move=1
types(shadow, kinetic)
removetrigger(endofturn,true,0)
trigger(onPlay, do(moveUnits((any),1,Player,away,$Move)))
trigger(onPlay, do(purge(self)))
text("Push an adjacent unit.\nPurge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/repulsiveshadow.png");

title(Rapid Shadow);
cost(0)
$Move=1
types(shadow, movement)
removetrigger(endofturn,true,0)
trigger(onPlay, do(movePlayer($Move)))
trigger(onPlay, do(purge(self)))
text("Move $Move .\nPurge")
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/rapidshadow.png");

title(Evasive Shadow);
cost(0);
$Status=10
types(shadow, dodge)
removetrigger(endofturn,true,0)
trigger(onPlay,do(addStatus(dodge,$Status)));
trigger(onPlay,do(purge(self)));
text("Gain $Status dodge.\nPurge");
modifiers(shadow)
rarity(0)
image("res://Images/CardArt/evasiveshadow.png");

title(After Image);
cost(1)
types(shadow)
$Block=1
text("When you play a card, gain $Block block")
trigger(play,do(block($Block)))
removetrigger(endofturn,true,3)
rarity(2)
image("res://Images/CardArt/Afterimage.png");

title(Midnight Breeze);
types(wind,shadow)
cost(1)
$Count=4
$Draw=1
$currentCount=0
text(For every $Count cards you play, draw $Draw . $currentCount played)
trigger(play,do(addVar(self,currentCount,1)))
trigger(play,if(($currentCount=$Count),(do(draw($Draw)))))
trigger(play,if(($currentCount=$Count),(do(setVar(self,currentCount,0)))))
removetrigger(endofturn,true,4)
rarity(4)
image("res://Images/CardArt/MidnightBreeze.png");

title(Sleight of Hand);
types(shadow);
cost(0);
trigger(onPlay,do(move(Hand,Play,(select(Hand,((not (hasModifier(self, unplayable)))),"Pick a card to put in play")))))
text("Put a card from your hand into play without playing it")
rarity(7);
removetrigger(endofturn,true,1)
image("res://Images/CardArt/SleightOfHand.png");

title(Wheel of Darkness);
types(shadow,mechanical)
cost(1);
trigger(startofturn, do(createByMod((shadow),Hand)))
text("At the start of your turn, add a shadow to your hand")
rarity(6);
removetrigger(endofturn,true,10);
image("res://Images/CardArt/WheelOfDarkness.png");

title(Knives in the Dark)
types(knife,shadow)
cost(1)
trigger(onCreate,if((hasModifier(argv[0],kitchenknife)),(do(createByMod((shadow),Hand,self,true)))));
trigger(onCreate,if((hasModifier(argv[0],shadow)),(do(createByMod((kitchenknife),Hand,self,true)))));
text("When you create a shadow, also get a kitchen knife, and vice versa")
removetrigger(onCreate,(+,(hasModifier(argv[0],kitchenknife)),(hasModifier(argv[0],shadow))),10);
rarity(2)
image("res://Images/CardArt/Knivesinthedark.png");

title(Piercing Root);
types(shadow, fungal,attack);
cost(0);
$Range=2
$Damage=1
$Increment=4
$CardTotal=7
$CardsPlayed=7
trigger(onPlay, do(damage, ($Damage,(shadow,piercing),(any),$Range)));
removetrigger(endofturn,true,1)
trigger(play, do(addVar(self,CardsPlayed,-1)))
trigger(play, if(($CardsPlayed=0),(do(addVar(self,Damage,$Increment)))));
trigger(play, if(($CardsPlayed=0),(do(move, (Play, Discard, self)))));
trigger(play, if(($CardsPlayed=0),(do(setVar( self, CardsPlayed,$CardTotal)))))
trigger(onRemoveFromPlay, do(setVar( self, CardsPlayed,$CardTotal)))
rarity(4)
text("Deal $Damage piercing shadow damage range $Range. If you play $CardsPlayed more cards this turn, increase this cards damage by $Increment and discard it from play")
image("res://Images/CardArt/PiercingRoot.png");

title("Light and Shadow");
cost(0);
types(light,shadow)
$Energy=1
text("Gain $Energy energy per shadow hard in your hand");
trigger(onPlay, do(gainEnergy,((count(Hand, shadow)))));
removetrigger(endofturn,true,2);
rarity(3);
image("res://Images/CardArt/lightandshadow.png");

title("Dark Coin")
cost(0);
types(shadow);
trigger(onPlay, do(setVar( (select(Hand,(if(($Cost>0),true)),"-",1,true)),Cost,0)))
removetrigger(endofturn,true,0);
text("Set the cost of a random card in your hand to 0 until played")
rarity(6);
image("res://Images/CardArt/DarkCoin.png");

title("Spreading Corruption")
cost(1);
types(shadow)
$Status=1
trigger(play, do(addStatus(corruption,1,(do( selectTiles((1, (any), "-friendly"),40, Player))))))
text("When you play a card, add 1 corrupt to a random enemy")
removetrigger(endofturn, true, 3)
rarity(5)
image("res://Images/CardArt/SpreadingCorruption.png");

##title("Recall");
##cost(0);
##types(shadow);
##text("Return the last card in play, other than Recall, to your hand");
##trigger(onPlay,do(move(

