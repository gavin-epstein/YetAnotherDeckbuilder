title("Wind Walker");
text("When you play a wind card, draw a card for each wind card in play");
trigger(play, if((hastype(argv[0],wind)),(do(draw,((count(Play, wind))),false))));
types(wind);
removetrigger(play,(hastype(argv[0],wind)),6);
image("res://Images/CardArt/WindWalk.png");
rarity(1);
cost(1);

title(Whirlwind);
$Draw=5
text("Discard your hand, draw $Draw cards");
cost(1);
trigger(onPlay, do(discardAll(false)));
trigger(onPlay, do(draw($Draw)));
types(wind, cloud);
rarity(4);
removetrigger(endofturn,true,1);
image("res://Images/CardArt/whirlwind.png");

title(Quickstep);
cost(0);
types(movement,wind,shadow);
rarity(10);
$Move=1
$Draw=1
removetrigger(endofturn,true,2);
trigger(onPlay, do(movePlayer,($Move),false))
trigger(onPlay, do(draw,($Draw),false))
text("Move $Move , draw $Draw")
image("res://Images/CardArt/Quickstep.png")

title("Windmill");
types(wind,mechanical);
$Draw=1
text("At the start of your turn, draw $Draw");
trigger(startofturn,do(draw($Draw)));
removetrigger(endofturn,true,7);
cost(2);
rarity(3);
image("res://Images/CardArt/Windmill.png")

title("Wind Blast");
types(wind,attack);
$Damage=2
$Range=40
text("Deal damage equal to (+,$Damage,0) times the number of cards in your hand, any range");
trigger(onPlay, do(damage((* ,$Damage,(countModifier(Hand,any))),(wind),(any),$Range)))
removetrigger(endofturn,true,1);
image("res://Images/CardArt/WindBlast.png")
cost(1);
rarity(5);

title("Sacred Breath");
types(wind);
modifiers(eternal,unique);
removetrigger(never, true,0);
cost(1);
text("Increase your maximum hand size by 2");
trigger(onPlay, do(addhandsize(2)));
rarity(1);
image("res://Images/CardArt/SacredBreath.png")

title("Steady Breathing")
types(wind, reaction);
cost(0)
$Draw=1
$DamageReduction= 2
text(The next time you are attacked, reducing incoming damage by $DamageReduction and draw $Draw);
trigger(onReaction, do(draw($Draw)))
trigger(onReaction, do(addVar(self, DamageTaken, (*, -1, $DamageReduction))));
removetrigger(onReaction, true, 1);
rarity(4);
image("res://Images/CardArt/steadybreathing.png")

title("Boomerang")
types(wind, attack)
$Damage=5
$Range=40
cost(0)
$DrawTotal=6
$Drawn=1
trigger(onPlay, do(damage, ($Damage,(slash),(any),$Range)));
trigger(draw,do(addVar(self, Drawn, argv[0])));
trigger(draw,if(($Drawn > $DrawTotal),(do(move(Play,Hand,self,0)))));
trigger(draw,if(($Drawn > $DrawTotal),(do(setVar(self, Drawn, 1)))));
trigger(draw, do(setVar(self, removecount, (+, -1, (+,(*,-1,Drawn),$DrawTotal)))))
text("Deal $Damage slash damage at any range. When you draw $removecount more cards, put this in your hand")
removetrigger(draw, false, 6)
image("res://Images/CardArt/Boomerang.png")
rarity(7);

#unceasing top
